# 公共 SD 基座：2026-09-25 状态

Qwen3 MoE 与 Qwen3.6-35B-A3B 已迁入公共路由、Graph和状态生命周期。这里记录ReplaySSM实施前的基座；**ReplaySSM尚未实现，质量等价也尚未成立**。

## 实现与入口

- 生产起点`8b66bf2`，最后生产提交`679816b`，正式测量快照`ea9afdf`；后续只集成独立测试和本文档。
- SD通过实际路由、attention与状态组件运行，不使用模型名字白名单；目前SD调度仍要求`legacy`。
- 保留普通SD、缓存路由、补缺加载、逐步成本自适应、verify专家预取与Graph。
- 状态保持配置预算，按本轮长度申请；可回收未锁定前缀状态，必要时缩短草稿或回退AR。最终输出终止位置确定后提交状态，归还其他临时槽。
- [SD使用说明](self-speculative-decoding.md)、[Graph说明](sd-cuda-graphs.md)、[独立验收入口](../blackbox_tests/sd_portability/README.md)。

生产阶段：`16ace29`公共路由、`41c09b1`组件Graph、`79f8f1d`状态提交、`7cdda4b`实际长度预算、`2e07ccb`naive槽隔离、`679816b`统一预取收尾。相对起点生产代码**+316/−206/净增110行**；独立测试与文档另计。

## 已验证的范围

- 两模型10配置矩阵：754项结构检查通过、0接口失败，9项文本差异保留。覆盖N1/3/8、k1/3/8、两种路由、三个开关和naive重建；各例实际执行draft/verify Graph。前三例来自`2e07ccb`，其余来自`679816b`。
- EOS、stop、取消、并发尾批和生成前缀复用已检查；5项公开CLI检查通过。
- 四并发状态压力：radix16槽、naive默认4槽均完整回退AR；显式naive20槽时，最大N8主要缩至N2，继续执行SD/Graph。三例分别278/276/300项通过，资源恢复成功。
- 现有harness配置的约32K输入完成三次请求，51项结构检查通过；实际长前缀命中32789 token，draft/verify Graph运行。简单口令首词格式判定失败，没有改判。
- 两模型全开用例没有触发draft补缺加载；Qwen3全开的预取载入7个但使用0个。不能据此宣称所有事件已覆盖。

## 质量与未完成项

固定8题：AR为5/8，SD为4/8。Python copy题AR正确、SD错误；重构前`8b66bf2`也有同一问题，同GPU复核仍出现。已定位到首次verify的logits排序翻转，但尚未定位到具体计算组件，不能称无害误差或质量等价。

AR自身也存在前缀命中与从头计算文本不同；SD差异同样保留。完整58请求agent trace、正式基线标定、随机采样分布验证没有在本轮完成。

用户随后指定性能轮不评质量。harness campaign显式跳过输出probe和文本一致性判定，仍检查执行完整与相同工作量；这是本轮评测规则，不代表上述问题已修复。

## C16快速性能结果

BF16 offload、Graph、legacy、缓存路由＋补缺＋逐步自适应，k3、最多8步、预取关闭。每臂16-token预热，两轮64-token计分，共2048计分token；共享CPU负载保留。

| 模型 | 同模型对照GPU / 专家池 | AR token/s | SD token/s | 提升 |
| --- | --- | ---: | ---: | ---: |
| Qwen3-30B-A3B | GPU0 / 14.994GiB | 22.97 | 27.41 | 19.3% |
| Qwen3.6-35B-A3B | GPU2 / 9GiB | 26.58 | 28.17 | 6.0% |

Qwen3重构前同协议为27.4057 token/s，当前为27.4096，差异+0.014%；草稿数、接受数和Graph重放数相同。Qwen3.6实际草稿均为1步，SD推进78.5%的decode输出。各模型内部同卡、同资源，不跨模型或跨卡计算加速比。

Qwen3.6默认96个状态槽，满C16活跃请求各持有3槽，共48槽；每个起草N步的请求另需N+2槽，所以剩48槽只容纳N1。容量限制发生在成本判断之前；`state_slot_stops=0`只说明没有因容量整批放弃SD，不表示没有缩短。更长草稿的收益尚未测量。

详细数字见[CSV](experiments/sd-c16-20260924.csv)。这是两轮短负载结果，不能替代完整agent trace。Qwen3.6首次旧客户端在预热后拒绝资源配置，未进入计分；计分两臂均使用通用独立客户端，重放相同冻结请求。

## 后续方向

讨论中的方案包括借用空闲前缀缓冲、减少状态复制、按实际长度选择Graph，以及保存小记录代替每位置完整状态的ReplaySSM路线。此前估算的显存节省是设计预算，不是实现或性能结果。

工作区详细报告目录为`reports/sd_common_runtime_20260924/`与`reports/sd_c16_portability_20260924/`。原始运行记录保留于本机`/data2/servebig-envs/`下对应`sd_common_runtime_20260924_gpu2`和`sd_c16_portability_20260924_gpu0`目录；原始大日志与trace不随本摘要发布。
